name: API workflow

on:
  # Triggers the workflow on push or pull request events but only for the "test" branch
  push:
    branches: [ "test" ]
  pull_request:
    branches: [ "test" ]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Test python API
    steps:
    - uses: actions/checkout@v4
    - name: Run tests and collect coverage

      run: |
        pip install pytest pytest-cov
<<<<<<< HEAD
        pytest --cov=. pass.py
=======
      run: 
=======
      run: |
>>>>>>> 4cedc07 (change actions):.github/workflows/test.yaml
        pip install pytest pytest-cov
        pytest --cov .
>>>>>>> d6aed47 (change actions):.github/workflows/test.yaml
=======
        pytest --cov=. fail.py myfile.py pass.py
>>>>>>> 86a1654 (change actions & files)
    - uses: codecov/codecov-action@v4
      with:
        fail_ci_if_error: true # optional (default = false)
        files: ./coverage1.xml,./coverage2.xml # optional
        flags: unittests # optional
        name: codecov-umbrella # optional
        token: ${{ secrets.CODECOV_TOKEN }} # required
        verbose: true # optional (default = false)